$( document ).ready(function() {

    /*$('.users_to_store').change(function() {
        if(this.checked) {
            var returnVal = confirm("Are you sure?");
            $(this).prop("checked", returnVal);
            //add state options to select
            $.ajax({
                method: "POST",
                url: "../stores/add_user_to_store/3/1",
            }).done(function( data ) {
                console.log(data);
            }).fail(function( data ) {
                console.error('Error:', data);
            });
        }
        $('#textbox1').val(this.checked);
    });*/

    //add state options to select
    /*$.ajax({
        method: "GET",
        url: "",
        dataType: 'json'
    }).done(function( data ) {
        console.log(data);
    }).fail(function( data ) {
        console.error('Error:', data);
    });*/

});